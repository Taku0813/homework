import 'package:flutter/material.dart';

class Bookmark3 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Bookmark3'),
      ),
      body: Container(
        color: Colors.blue,
      ),
    );
  }
}
